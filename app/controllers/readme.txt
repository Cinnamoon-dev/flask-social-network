Nesse módulo/diretório serão guardados as configs prinicipais do flask
Dentro do controllers vamos salvar as nossas páginas que são a lógica do nosso site

Inside this module/package, the main flask's configs will be stored and the logic parts. So the routers will also come
here, since they are the pages that are the logic part of the application